
let nombre
let apellido

// Comprobar si una varible tiene valor
// console.log(`${nombre || "No tengo nombre"} ${apellido}`);

const isActive = true

// Operador ternario para condiciones simples
const message = (isActive) 
                    ? "Active" 
                    : "No active"

console.log(message);